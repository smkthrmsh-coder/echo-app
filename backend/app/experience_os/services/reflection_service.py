"""ReflectionService — interface stub only. No reflection system exists yet to wrap."""

from __future__ import annotations


class DefaultReflectionService:
    def is_enabled(self) -> bool:
        return False
